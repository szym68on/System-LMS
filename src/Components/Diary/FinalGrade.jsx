function FinalGrade({ bgColor }) {
  return (
    <p
      className={`w-[100%] text-center px-2 py-5 text-lg bg-${bgColor} border-l-[1px] border-white`}
    >
      5
    </p>
  );
}

export default FinalGrade;
